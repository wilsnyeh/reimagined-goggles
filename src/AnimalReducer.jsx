import { useReducer } from "react";

function AnimalReducer() {
    const [breedList, dispatch] = useReducer((breedListReducer, initialBreedList) => {

})
}
// const [breedList, setBreedList] = useState([]);
